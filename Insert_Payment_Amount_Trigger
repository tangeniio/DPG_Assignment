
CREATE TRIGGER  Insert_Payment_Amount ON Registration

   AFTER INSERT
AS 

SET NOCOUNT ON

DECLARE @Reg_ID INT;
SELECT @Reg_ID = ID.Reg_ID FROM inserted ID;

INSERT INTO Event_Payment(Payment_No,Payment_Amount)
VALUES(@Reg_ID,500);
